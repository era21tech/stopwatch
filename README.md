# stopwatch
A simple web-based stopwatch made with HTML, CSS and JavaScript. Includes start, stop and reset controls with a clean glassmorphism-style UI and digital time display.

4## 💡 Features

- Digital stopwatch display in `HH:MM:SS` format  
- **Start** the timer  
- **Stop / Pause** the timer  
- **Reset** back to `00:00:00`  
- Simple, responsive UI using pure CSS  
- Background image with dark glass effect for modern look

---

## 🧱 Tech Stack

- **HTML5** for structure   
- **CSS3** for styling and layout 
- **Vanilla JavaScript** for stopwatch logic (start, stop, reset, time updates)

---

# 📂 Project Structure

```text
.
├── index.html      # Main stopwatch page
├── style.css       # Styling for stopwatch & buttons
└── images/
    ├── background.png
    ├── start.png
    ├── stop.png
    └── reset.png
````

* `index.html` contains the stopwatch layout: time display and three image buttons. 
* `style.css` handles the center box, background image, fonts and button sizes. 

---

## 🚀 How to Run

1. Download or clone this repository:

   ```bash
   git clone https://github.com/your-username/your-repo-name.git
   ```
2. Make sure the `images` folder contains:

   * `background.png`
   * `start.png`
   * `stop.png`
   * `reset.png`
3. Open `index.html` in any web browser (Chrome, Edge, Firefox, etc.).

No server or build tools required — it runs directly in the browser.

---


## 📌 Future Improvements (Ideas)

You can add these later:

* Lap time recording
* Save last time in `localStorage`
* Keyboard shortcuts for start/stop/reset
* Sound when starting or stopping
* Mobile-optimized buttons with hover/active effects

---

## ⭐ Contributing / Feedback

This is a beginner-friendly project made for learning basic **JavaScript DOM + timing functions**.
If you like it or learned something, consider:

* Giving the repo a ⭐ on GitHub
* Forking it and experimenting with new features
* Sharing suggestions or issues in the **Issues** section

